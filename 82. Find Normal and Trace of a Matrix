#include <stdio.h>
#include <math.h>
int main(){
    int a[2][2]={{3,4},{2,1}};
    int trace=a[0][0]+a[1][1];
    double normal = sqrt(a[0][0]*a[0][0]+a[0][1]*a[0][1]+a[1][0]*a[1][0]+a[1][1]*a[1][1]);
    printf("Trace = %d\nNormal = %.2f", trace, normal);
    return 0;
}
