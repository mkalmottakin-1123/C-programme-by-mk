#include <stdio.h>
int main() {
    int a[] = {1,1,2,2,3,4,4,5}, n = 8;
    int j = 0;
    for(int i=0; i<n-1; i++)
        if(a[i] != a[i+1]) a[j++] = a[i];
    a[j++] = a[n-1];
    printf("After removing duplicates: ");
    for(int i=0; i<j; i++) printf("%d ", a[i]);
    return 0;
}
