#include <stdio.h>
int main(){
    char tmpname[] = "tempfileXXXXXX";
    int fd = mkstemp(tmpname);
    if(fd == -1) { perror("mkstemp"); return 1; }
    printf("Temp file: %s\n", tmpname);
    close(fd);
    remove(tmpname);
    return 0;
}
