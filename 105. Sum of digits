#include <stdio.h>

int main() {
    char str[200];
    printf("Enter a sentence: ");
    gets(str);

    printf("Sub-strings:\n");

    for (int i = 0; str[i] != '\0'; i++) {
        if (str[i] == ' ')
            printf("\n");
        else
            printf("%c", str[i]);
    }

    return 0;
}
