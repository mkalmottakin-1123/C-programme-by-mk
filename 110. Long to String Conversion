#include <stdio.h>
int main() {
    long x; char s[50];
    printf("Enter long: ");
    if(scanf("%ld",&x)!=1) return 0;
    snprintf(s, sizeof(s), "%ld", x);
    printf("String: %s\n", s);
    return 0;
}
