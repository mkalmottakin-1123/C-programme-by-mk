#include <stdio.h>
#include <stdlib.h>
int main() {
    char s[100];
    printf("Enter number string: ");
    if(scanf("%99s", s)!=1) return 0;
    long val = strtol(s, NULL, 10);
    printf("Long: %ld\n", val);
    return 0;
}
