#include <stdio.h>
#include <stdlib.h>
int main() {
    char s[50];
    printf("Enter octal number: ");
    if(scanf("%49s", s)!=1) return 0;
    long dec = strtol(s, NULL, 8);
    printf("Decimal: %ld\n", dec);
    return 0;
}
