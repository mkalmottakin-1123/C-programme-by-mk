#include <stdio.h>
int main() {
    double d; char s[100];
    printf("Enter double: ");
    if(scanf("%lf",&d)!=1) return 0;
    snprintf(s,sizeof(s), "%lf", d);
    printf("String: %s\n", s);
    return 0;
}
