#include <stdio.h>
#include <math.h>
int main(){
    int r=8;
    for(int y = -r; y<=r; y++){
        for(int x=-r; x<=r; x++){
            if (fabs(sqrt(x*x + y*y) - r) < 0.8) putchar('*');
            else putchar(' ');
        }
        putchar('\n');
    }
    return 0;
}
