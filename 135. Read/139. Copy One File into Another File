#include <stdio.h>
int main(){
    FILE *in=fopen("src.txt","rb"), *out=fopen("dst.txt","wb");
    if(!in||!out){ perror("fopen"); return 1; }
    char buf[4096]; size_t n;
    while((n=fread(buf,1,sizeof(buf),in))>0) fwrite(buf,1,n,out);
    fclose(in); fclose(out);
    return 0;
}
