#include <stdio.h>
int main(){
    FILE *src=fopen("a.txt","r"), *dst=fopen("b.txt","a");
    if(!src||!dst){ perror("fopen"); return 1; }
    char c;
    while((c=fgetc(src))!=EOF) fputc(c,dst);
    fclose(src); fclose(dst);
    return 0;
}
