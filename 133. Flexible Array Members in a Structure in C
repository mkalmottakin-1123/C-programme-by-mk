#include <stdio.h>
#include <stdlib.h>
struct S{ int n; char data[]; };
int main(){
    int len = 6;
    struct S *p = malloc(sizeof(struct S) + len);
    p->n = len;
    memcpy(p->data, "Hello!", len);
    printf("%s\n", p->data);
    free(p);
    return 0;
}
