#include <stdio.h>
int main() {
    float f;
    char s[50];
    printf("Enter float: ");
    if(scanf("%f",&f)!=1) return 0;
    snprintf(s, sizeof(s), "%f", f);
    printf("String: %s\n", s);
    return 0;
}
