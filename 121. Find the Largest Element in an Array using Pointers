#include <stdio.h>
int main(){
    int a[] = {3, 7, 2, 9, 5};
    int n = sizeof(a)/sizeof(a[0]);
    int *p = a;
    int max = *p;
    for(int i=1;i<n;i++) if(*(p+i) > max) max = *(p+i);
    printf("Max = %d\n", max);
    return 0;
}
